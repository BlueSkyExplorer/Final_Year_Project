experiment_name: multiclass_resnet18_ce
model:
  paradigm: multiclass
  backbone: resnet18
  loss: ce
training:
  batch_size: 16
  num_epochs: 40
  learning_rate: 1e-4
  weight_decay: 1e-4
cv:
  num_folds: 5
  current_fold: 0
paths:
  dataset_config: configs/dataset/limuc.yaml
  output_root: results
