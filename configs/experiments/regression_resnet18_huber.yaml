experiment_name: regression_resnet18_huber
model:
  paradigm: regression
  backbone: resnet18
  loss: huber
  delta: 1.0
training:
  batch_size: 16
  num_epochs: 40
  learning_rate: 1e-4
  backbone_learning_rate: 5e-5
  freeze_epochs: 5
  weight_decay: 1e-4
cv:
  num_folds: 5
  current_fold: 0
paths:
  dataset_config: configs/dataset/limuc.yaml
  output_root: results
